import React from "react";

const InvoiceItemInput = ({ id }) => {
  console.log(id);

  return (
    <>
      {/* <div>
        <span>Item Name</span>
        <span>Qty</span>
        <span>Price</span>
        <span>Total</span>
        <span></span>
      </div>
      <div>
        <span>{items.name}</span>
        <span>{items.quantity}</span>
        <span>{items.price}</span>
        <span>{items.total}</span>
        <span></span>
      </div> */}
    </>
  );
};

export default InvoiceItemInput;
