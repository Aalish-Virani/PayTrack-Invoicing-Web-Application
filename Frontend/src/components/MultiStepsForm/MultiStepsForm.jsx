import React from "react";

const MultiStepsForm = ({ steps, children }) => {

  return (
    <section className="">
      <div></div>
      <div>{{children}}</div>
    </section>
  );
};

export default MultiStepsForm;
